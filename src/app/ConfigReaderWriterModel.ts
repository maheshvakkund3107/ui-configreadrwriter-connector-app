export class ConfigReaderWriterModel{
    configReader:string
    configWriter:string
    constructor(
        configReader:string,
        configWriter:string
    ){
        this.configReader=configReader
        this.configWriter=configWriter
    }
        
}